name: CI

on:
  pull_request:
    branches: [ master]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v6
      
    - name: Run Tests
      run: |
        chmod +x ./build_tests.sh
        ./build_tests.sh